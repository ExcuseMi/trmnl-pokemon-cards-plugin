services:
  test-transform:
    build: ./test/transform
    container_name: trmnl-pokemon-cards-test-transform
    profiles:
      - test
    volumes:
      - ./plugin/src/transform.js:/app/transform.js:ro,z
      - ./test/transform/data:/app/data:ro,z
    env_file: .env
